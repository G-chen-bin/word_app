<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">


    <RadioGroup
        android:id="@+id/rg_tab_bar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_alignParentBottom="true"
        android:background="@color/bg_white"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/rb_review"
            style="@style/tab_menu_item"
            android:drawableTop="@drawable/tab_menu_review"
            android:text="@string/tab_menu_review" />

        <RadioButton
            android:id="@+id/rb_book"
            style="@style/tab_menu_item"
            android:drawableTop="@drawable/tab_menu_book"
            android:text="@string/tab_menu_book" />

        <RadioButton
            android:id="@+id/rb_statistic"
            style="@style/tab_menu_item"
            android:drawableTop="@drawable/tab_menu_statistic"
            android:text="@string/tab_menu_statistic" />

        <RadioButton
            android:id="@+id/rb_setting"
            style="@style/tab_menu_item"
            android:drawableTop="@drawable/tab_menu_setting"
            android:text="@string/tab_menu_setting" />

    </RadioGroup>

    <View
        android:id="@+id/div_tab_bar"
        android:layout_width="match_parent"
        android:layout_height="2px"
        android:layout_above="@id/rg_tab_bar"
        android:background="@color/div_white" />

    <android.support.v4.view.ViewPager
        android:id="@+id/vpager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/div_tab_bar"
        />


</RelativeLayout>
